# Tokens for HTTP authentication (comma-separated).
# Leave empty to disable auth (local testing only).
CARTOGRAPHER_TOKENS=

# Path to the age key file for SOPS (only if the KB uses encrypted secrets).
SOPS_AGE_KEY_FILE=./age-key.txt
